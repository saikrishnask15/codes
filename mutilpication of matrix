#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int t,k;
    scanf("%d",&t);
    while(t--){
        int n1,m1,n2,m2;
        scanf("%d%d",&n1,&m1);
        int a[n1][m1];
        for(int i=0;i<n1;i++){
            for(int j=0;j<m1;j++){
                scanf("%d",&a[i][j]);
            }
        }
         scanf("%d%d",&n2,&m2);
        int b[n2][m2];
         for(int i=0;i<n2;i++){
            for(int j=0;j<m2;j++){
                scanf("%d",&b[i][j]);
            }
        }
        int mul[n1][m2];
         for(int i=0;i<n1;i++){
            for(int j=0;j<m2;j++){
                 mul[i][j]=0;
                for(k=0;k<n2;k++)
                {
                mul[i][j]+=a[i][k]*b[k][j];
                }
            }
        }
        for(int i=0;i<n1;i++){
            for(int j=0;j<m2;j++){
                printf("%d ",mul[i][j]);
            }
            printf("\n");
        }
        
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
